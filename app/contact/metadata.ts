import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Contact",
  description: "Start a project with alar.dev. Tell us about your goals — we respond within 24 hours.",
};
